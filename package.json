{
  "name": "headcount-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Real-time concurrent-visitor counting on Cloudflare Workers + Durable Objects. No backend, no database server.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.107.0"
  }
}
